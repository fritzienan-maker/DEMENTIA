const fs = require('fs');echo let html = fs.readFileSync('index.html', 'utf8');echo html = html.replace('__VITE_API_KEY__', process.env.VITE_API_KEY || '');echo html = html.replace('__VITE_LOCATION_ID__', process.env.VITE_LOCATION_ID || '');echo fs.mkdirSync('dist', { recursive: true });echo fs.writeFileSync('dist/index.html', html);echo console.log('Build complete. Keys injected.');
